---
// Root path — redirect to default locale (BG).
// Astro's i18n redirectToDefaultLocale will normally handle this, but
// this explicit fallback guarantees the behaviour on all hosts.
return Astro.redirect('/bg', 302);
---
